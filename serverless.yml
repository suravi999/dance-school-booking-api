service: dance-school-booking-service
frameworkVersion: '4.21'

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs20.x

functions:
  getAllClasses:
    handler: src/handlers/classes.getAllClasses
    events:
      - httpApi:
          path: /classes
          method: get

  bookClass:
    handler: src/handlers/bookClass.bookClass
    events:
      - httpApi:
          path: /classes/{id}/book
          method: post